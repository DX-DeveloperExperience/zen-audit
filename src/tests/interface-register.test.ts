import { StackRegister } from '../interface-register';

// import { StackRegister } from '../interface-register';

// interface DummyInterface {
//   dummyMethod(): any;
// }

// test('number of registerd classes is three', () => {
//   @StackRegister.register
//   class DummyImpl1 {}
//   @StackRegister.register
//   class DummyImpl2 {}
//   @StackRegister.register
//   class DummyImpl3 {}
//   expect(StackRegister.getImplementations.length).toEqual(3);
// });

// test('return empty array if no class of this type is registered', () => {
//   expect(StackRegister.getImplementations.length).toEqual(0);
// });

// test('throws an exception when registering a class for an unexisting interface', () => {
//   @StackRegister.register('Unexisting')
//   class Unexisting1 {}
//   // expect(StackRegister.register('Unexisting')).toThrowError(
//   //   'Unexisting interface "Unexisting"',
//   // );
// });
